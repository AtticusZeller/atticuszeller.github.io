# atticuszeller notes

Obsidian vault. Open this directory directly in Obsidian.

- `main` branch — pure vault content
- `page` branch — mkdocs build pipeline
- `gh-pages` branch — generated site (do not edit)
- Site: <https://docs.atticux.me/>

Real `.git` directory lives at `99_system/git/atticuszeller.git/`,
rooted via gitfile to keep it out of vault sync.
